# Bukti Serving

Tambahkan screenshot bukti serving model di folder ini, misalnya:

- API FastAPI/uvicorn berjalan.
- `GET /health` mengembalikan `artifacts_ready: true`.
- `GET /features` berhasil.
- `POST /predict` berhasil mengembalikan prediksi.

Nama file yang disarankan:

- `1.serving_api_running.png`
- `2.health_endpoint.png`
- `3.predict_endpoint.png`
